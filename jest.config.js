module.exports = {
    testEnvironment: 'node',
    // Hentikan tes setelah kegagalan pertama (opsional)
    bail: true, 
    // Timeout untuk tes async (misal: 30 detik)
    testTimeout: 30000, 
  };